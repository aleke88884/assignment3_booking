# 🐳 Инструкция по запуску SmartBooking с Docker и PostgreSQL

## 📋 Что было сделано

Я настроил ваш проект для работы с Docker и PostgreSQL:

### 1. **Dockerfile**
Многоступенчатая сборка Go приложения:
- Первый этап: компиляция Go приложения
- Второй этап: создание минимального образа с Alpine Linux
- Результат: маленький и быстрый Docker образ

### 2. **docker-compose.yml**
Настройка двух сервисов:
- **postgres**: база данных PostgreSQL 16
- **app**: ваше Go приложение

### 3. **Обновленный код**
- `config/config.go` - теперь читает переменные окружения
- `main.go` - подключается к PostgreSQL
- Все репозитории (`user_repository.go`, `resource_repository.go`, `booking_repository.go`) - переписаны для работы с PostgreSQL вместо in-memory хранилища

### 4. **Файлы конфигурации**
- `.env` - переменные окружения
- `.dockerignore` - исключает лишние файлы при сборке
- `Makefile` - команды для удобной работы

## 🚀 Как запустить проект

### Способ 1: Используя Makefile (Рекомендуется)

```bash
# Запустить проект
make up

# Посмотреть логи
make logs

# Остановить проект
make down

# Перезапустить
make restart

# Удалить все (включая данные БД)
make clean
```

### Способ 2: Используя docker-compose напрямую

```bash
# Собрать и запустить
docker-compose up -d

# Посмотреть логи
docker-compose logs -f

# Остановить
docker-compose down

# Остановить и удалить данные
docker-compose down -v
```

## 📊 После запуска

Когда вы запустите проект, будут доступны:

- **API приложения**: http://localhost:8080
- **Swagger документация**: http://localhost:8080/swagger/
- **Health check**: http://localhost:8080/health
- **PostgreSQL**: localhost:5432

## 🗄️ Работа с базой данных

### Подключиться к PostgreSQL

```bash
# Через Makefile
make db-shell

# Или напрямую
docker-compose exec postgres psql -U postgres -d smartbooking
```

### Посмотреть таблицы

```sql
-- В psql выполните:
\dt

-- Посмотреть данные в таблице users
SELECT * FROM users;

-- Посмотреть данные в таблице resources
SELECT * FROM resources;

-- Посмотреть данные в таблице bookings
SELECT * FROM bookings;
```

## 🔧 Настройка переменных окружения

Отредактируйте файл `.env` если нужно изменить настройки:

```bash
# База данных
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=smartbooking
DB_HOST=postgres
DB_PORT=5432

# Сервер
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
```

## 📝 Структура проекта

```
assignment3/
├── Dockerfile              # Сборка Go приложения
├── docker-compose.yml      # Настройка сервисов
├── .env                    # Переменные окружения
├── .dockerignore          # Исключения при сборке
├── Makefile               # Удобные команды
├── migrations/            # SQL миграции
│   └── 001_create_tables.sql
├── config/
│   └── config.go          # Конфигурация (читает .env)
├── internal/
│   ├── database/
│   │   └── database.go    # Подключение к PostgreSQL
│   └── repository/        # Репозитории с PostgreSQL
│       ├── user_repository.go
│       ├── resource_repository.go
│       └── booking_repository.go
└── main.go                # Точка входа
```

## 🔍 Полезные команды

```bash
# Посмотреть запущенные контейнеры
docker-compose ps

# Посмотреть логи PostgreSQL
make logs-db

# Посмотреть логи приложения
make logs

# Зайти в shell контейнера приложения
make app-shell

# Перезапустить только приложение
docker-compose restart app

# Перезапустить только БД
docker-compose restart postgres

# Пересобрать проект с нуля
make rebuild
```

## 🧪 Тестирование API

После запуска можете протестировать API:

```bash
# Регистрация пользователя
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test User",
    "email": "test@example.com",
    "password": "password123",
    "role": "user"
  }'

# Создание ресурса
curl -X POST http://localhost:8080/api/resources \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Meeting Room A",
    "description": "Conference room",
    "capacity": 10
  }'

# Получить все ресурсы
curl http://localhost:8080/api/resources
```

## ❓ Часто задаваемые вопросы

### Порт уже занят?

Если порт 8080 или 5432 уже занят, измените в `.env`:

```bash
SERVER_PORT=3000
DB_PORT=5433
```

### Нужно пересоздать базу данных?

```bash
make clean    # Удалить все данные
make up       # Запустить заново
```

### Как обновить код приложения?

```bash
docker-compose restart app   # Если изменили код
# ИЛИ
make rebuild                # Если изменили зависимости
```

## ✅ Готово!

Теперь ваш проект работает с Docker и PostgreSQL!

Все данные сохраняются в PostgreSQL и будут доступны даже после перезапуска контейнеров.
